## Netrack
- Nc or netcat is used for debugging, port scans, and much more. It is knows as the network swiss army knife. 
- Limited but manual atlernative to nmap
-How does it work?
    - `nc “hostname” “port number”`
- You’ll be prompted with a blank screen, writing `help` will show you a list of commands. 
- What is the version
    - Type: `version`
- What is the flag?
    - Type: `list` > to get the directory listing
    Type: `get [file name]` > will paste what is in the file
- What is the size of the largest file in bytes? 
- For example one character = 1 byte
Going through all the files using `get count` the letters in each file > which ever file has the most characters including spaced > 35 letters > 5 spaces
    = 40 bytes